{
    "version": 2,
    "builds": [
        {
            "src": "./index.js",
            "use": "@vercel/node"
        }
    ],

    "source": "/api/(.*)",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/"
        }
    ]
}
